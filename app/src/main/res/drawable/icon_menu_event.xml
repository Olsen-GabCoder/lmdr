<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Couche 1 : Le fond -->
    <!-- Nous utilisons une autre couleur de notre palette, par exemple le rouge. -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/menu_icon_bg_red"/>
            <corners android:radius="12dp"/>
        </shape>
    </item>

    <!-- Couche 2 : L'icône -->
    <!-- Nous superposons l'icône de calendrier, maintenant corrigée. -->
    <item
        android:drawable="@drawable/ic_menu_plan_session"
        android:gravity="center" />

</layer-list>