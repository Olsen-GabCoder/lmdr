<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Couche 1 : Le fond -->
    <!-- Nous utilisons la septième couleur de notre palette, le jaune. -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/menu_icon_bg_yellow"/>
            <corners android:radius="12dp"/>
        </shape>
    </item>

    <!-- Couche 2 : L'icône -->
    <!-- Nous superposons l'icône de débat, maintenant corrigée. -->
    <item
        android:drawable="@drawable/ic_menu_debate"
        android:gravity="center" />

</layer-list>