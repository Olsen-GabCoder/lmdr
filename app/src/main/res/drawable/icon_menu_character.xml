<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Couche 1 : Le fond -->
    <!-- Nous utilisons de nouveau une couleur de notre palette, par exemple le bleu. -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/menu_icon_bg_blue"/>
            <corners android:radius="12dp"/>
        </shape>
    </item>

    <!-- Couche 2 : L'icône -->
    <!-- Nous superposons l'icône de personnage, maintenant corrigée. -->
    <item
        android:drawable="@drawable/ic_menu_favorite_character"
        android:gravity="center" />

</layer-list>