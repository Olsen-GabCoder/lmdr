<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Couche 1 : Le fond -->
    <!-- Nous utilisons la huitième couleur de notre palette, le rose. -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/menu_icon_bg_pink"/>
            <corners android:radius="12dp"/>
        </shape>
    </item>

    <!-- Couche 2 : L'icône -->
    <!-- Nous superposons l'icône de suggestion, maintenant corrigée. -->
    <item
        android:drawable="@drawable/ic_menu_guided_suggestion"
        android:gravity="center" />

</layer-list>