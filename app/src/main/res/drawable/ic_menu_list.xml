<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Couche 1 : Le fond -->
    <!-- Nous utilisons la quatrième couleur de notre palette, le cyan. -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/menu_icon_bg_cyan"/>
            <corners android:radius="12dp"/>
        </shape>
    </item>

    <!-- Couche 2 : L'icône -->
    <!-- Nous superposons l'icône "ajouter à la liste" que vous venez de me fournir. -->
    <item
        android:drawable="@drawable/ic_menu_add_to_list"
        android:gravity="center" />

</layer-list>